
{% extends 'base.html.twig' %}

{% block body %}
<div class="position-relative">
<div class="d-flex align-items-end">
  <div class="position-absolute d-flex flex-column align-items-start headline ms-5 ps-5">
    <div style="z-index: 2;">
    <h1 class="display-3 fw-bold text-light ">Zoo Arcadia</h1>
        <h2 class="display-6 fw-bold text-light">Un écoparc animalier aux portes de Brocéliande.</h2>
          <div class="mt-5">
            <a href="{{ path('app_home') }}#decouvrir" class="cta-button mt-3">Découvrir</a>
          </div>
    </div>
  </div>
  <div class="position-relative">
    <img src="{{ asset ('build/images/cover_index1.jpg')}}" class="w-100 h-100 img-fluid d-block" alt="Un tigre allongé sur une large souche d'abre coupé">
      <div class="position-absolute bottom-0 start-0 end-0"
          style="height: 60%; background: linear-gradient(to top, rgba(0,0,0,0.7), transparent);">
      </div>
  </div>
</div>

<div class="bg-arcadia-dark position-relative shadow-arcadia-dark">
<section class="container-fluid ">
  <h2 class="fs-1 fw-bold text-center py-5 text-light">18 espèces animales réparties sur 3 habitats</h2>
  <div class="container" id="decouvrir">
    <div class="row text-center">
        {% for habitat in habitats %}
        {% include "partials/_section.habitats.html.twig" %}
        {% endfor %}
    </div>
  </div>
  <div class="d-flex justify-content-center">
    <a href="{{ path('app_habitats') }}" class="btn btn-primary my-5">Voir tous les habitats</a>
  </div>
</section>

<section class="container">
  <h2 class="display-4 fw-bold text-center my-5 py-5 text-light">Services</h2>
  <div class="container">
    <div class="row text-center">
        {% for service in services %}
        {% include "partials/_section.services.html.twig" %}
        {% endfor %}
    </div>
  </div>
  <div class="d-flex justify-content-center">
    <a href="{{ path('app_services') }}" class="btn btn-primary my-5">Voir tous les Services</a>
  </div>
</section>


</div>
</div>
{% endblock %}